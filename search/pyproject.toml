[project]
name = "find"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "prompt-toolkit>=3.0.52",
]

[project.scripts]
search = "search.cli:main"
goto-picker = "search.cli:goto_main"

[tool.uv]
package = true
