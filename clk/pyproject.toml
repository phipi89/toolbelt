[project]
name = "clk"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["dash>=2.18.0", "plotly>=5.24.0"]


[project.scripts]
clk = "clk.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/clk"]
