[project]
name = "facts"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
facts = "facts.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/facts"]
